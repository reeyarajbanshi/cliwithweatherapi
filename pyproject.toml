[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "resolution_week2_Reeya"
version = "0.0.1"
dependencies = []

[project.scripts]
todo = "resolution_week2_Reeya.main:main"